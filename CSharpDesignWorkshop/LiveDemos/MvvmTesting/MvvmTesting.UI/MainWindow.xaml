<Window x:Class="MvvmTesting.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvvmTesting.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="450">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Zahl 1:" />
        <TextBlock Text="Zahl 1:" Grid.Row="1" />

        <TextBox Grid.Column="1" Text="{Binding Path=Number1}" />
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Path=Number2}" />

        <Button Grid.Row="2" Grid.ColumnSpan="2" Command="{Binding Path=CalculateCommand}">Add</Button>

        <TextBlock Grid.Row="3" Text="{Binding Path=Result}" />
    </Grid>
</Window>

---
description: 
globs: 
alwaysApply: true
---
Note that the Rust `rand` crate has recently been updated:

* Rename `rand::thread_rng()` to `rand::rng()`* 
* Rename fns `Rng::gen_range` to `random_range`, `gen_bool` to `random_bool`, `gen_ratio` to `random_ratio`
